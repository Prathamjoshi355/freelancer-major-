import { PageContainer } from "@/components/page"
export default function MyProposalsPage() {
  return <PageContainer title="My Proposals" description="Track pending, accepted, and rejected proposals." />
}
